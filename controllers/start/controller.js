const express = require('express');
const router = express.Router();
const path = require('path');

// router.get('/', (req, res) => {
//     res.sendFile(path.join(process.env.APP, 'Core/hub.json'))
// })

module.exports = router;